# 4.10
- 模型结构
- 训练推理情况
- 接下来的想法
	解决现在模型0分的问题(loss设计，注意力交互，流匹配(**两个流匹配还是一个？？？**))
	完成第二种模型结构并且对代码进行规范化，可以一键运行并整齐记录所有需要用到的数据
	
	梳理实验
- 问题
	1.原始代码中在训练时只加载了vlm的权重没有加载exper权重
	因为arm_expert的输出和之前不一样，不能加载之前expert的权重给arm_expert
	所以就是两个expert都要训
	2.根据测试视频来看，夹爪和手臂没有交流是不行的？
	3.loss是加还是拼接
	4.两个流匹配还是一个？？
	5.门控注意力？